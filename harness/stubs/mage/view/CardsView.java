package mage.view;

import java.util.LinkedHashMap;
import java.util.UUID;

/** Same shape as XMage's: a map of full card views, keyed by card id. */
public class CardsView extends LinkedHashMap<UUID, CardView> {
}
